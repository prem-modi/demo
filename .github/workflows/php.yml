name: Test Ngrok API

on:
  push:
    branches:
      - main

jobs:
  test_ngrok_api:
    runs-on: ubuntu-latest

    steps:

    - name: Make API Request to Ngrok
      run: |
        sleep 10  # Wait for ngrok to start
        curl http://localhost:4040/api/tunnels
